{
  "id":"${jobId}",
  "name": "${jobName}",
  "vendor": "${vendor}",
  "system": "${system}",
  "edition": "${edition}",
  "subVersion": "${subVersion}",
  "cron":"${cron}",
  "cronDesc":"${cronDesc}",
  "desc":"${desc}"
}
